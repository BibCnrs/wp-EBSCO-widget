{
    "name": "bibcnrs/wp-ebsco-widget",
    "description": "Wordpress plugin for EBSCO widget",
    "version": "0.9.1",
    "license": "CeCILL",
    "type": "wordpress-plugin",
    "require-dev": {
        "phpunit/phpunit": "4.8.*"
    },
    "require": {
        "php": ">=5.3.0"
    },
    "scripts": {
        "post-update-cmd": [
            "npm install --no-dev"
        ],
        "post-install-cmd": [
            "npm install --no-dev"
        ]
    }
}
